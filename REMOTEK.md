# Cosa cambia in questo fork rispetto a upstream

Base upstream: **rustdesk-api v2.7** (lejianwen, tag `v2.7`, ultimo commit
2025-09-29). Upstream e' fermo: questo fork e' il progetto (ADR-0005), quindi
l'elenco crescera'. `git log v2.7..remotek` mostra la stessa lista come commit.

| File | Modifica | Motivo | ADR |
|---|---|---|---|
| `.github/workflows/build.yml`, `build_test.yml` | rimossi | partivano su tag `v*.*.*`/`test*` e pubblicavano su registry altrui con azioni non piu' eseguite | 0006 |
| `LICENSE` | riga "Modifiche (c) 2026 Infinitek S.r.l." sotto l'attribuzione originale | MIT: attribuzione a lejianwen conservata | 0005 |
| `SECURITY.md`, `REMOTEK.md`, `CHANGELOG.md`, `.github/pull_request_template.md` | documenti del fork | sicurezza, tracciabilita' | REGOLE 13 |
| `CLAUDE.md` | estratto delle regole per l'agente che lavora solo su questo repo (sessioni cloud) | le regole complete stanno nel repo privato, che da qui non si legge | REGOLE 4, 14 |
| `go.mod`, `.gitignore`, `go.sum` | Go 1.26 / toolchain go1.26.8; `go.sum` versionato (generato dal job `gosum` della CI) | build riproducibile su una release di Go supportata | REGOLE 9 |
| `.github/workflows/remotek-ci.yml` (job `govulncheck`) | govulncheck v1.8.0 a versione fissata, senza `continue-on-error`: il job fallisce sulle vulnerabilita' note chiamate dal codice | una vulnerabilita' raggiunta si vede rossa sulla MR, non dopo il rilascio; ferma il merge solo quando `govulncheck` e' tra i controlli obbligatori del ruleset `remotek protetto` (si aggiunge dopo il merge, come `lint`) | REGOLE 6.3, 8 |
| `go.mod`, `go.sum` (dipendenze) | sei moduli alzati per chiudere una vulnerabilita' (`x/text`, `pgx/v5`, `go-jose/v4`, `logrus`, `jwt/v5` alla versione minima che corregge; `x/net` a v0.56.0, oltre il minimo v0.38.0, perche' la impone la selezione delle versioni con gli altri moduli); `x/crypto`, `x/sys`, `x/sync`, `x/tools` salgono di conseguenza; nessun salto di major | chiudono le 6 vulnerabilita' chiamate dal codice trovate da govulncheck (elenco in `CHANGELOG.md`) | REGOLE 8, 9 |
| `go.mod`, `go.sum` (gin) | `github.com/gin-gonic/gin` v1.9.0 -> v1.9.1, ultima patch della 1.9; salgono al minimo che gin chiede `sonic`, `go-json`, `cpuid/v2`, `go-isatty`, `go-toml/v2`, `ugorji/go/codec`, `x/arch` | chiude GHSA-2c4m-59x9-fr2g (avviso Dependabot) | REGOLE 8, 9 |
| `go.mod`, `go.sum` (oauth2) | `golang.org/x/oauth2` v0.23.0 -> v0.27.0, versione minima che corregge; nessun altro modulo sale | chiude GHSA-6v2p-p543-phr9 (avviso Dependabot) | REGOLE 8, 9 |
| `go.mod`, `go.sum` (x/image) | `golang.org/x/image` v0.13.0 -> v0.41.0 come indiretta fissata in `go.mod`; `mojocn/base64Captcha` resta v1.3.6, perche' anche la sua ultima versione (v1.3.8) chiede solo x/image v0.23.0; nessun altro modulo sale | chiude GHSA-9phm-fm57-rhg8, GHSA-44p7-9xx4-hf2g, GHSA-q675-qj96-32m9 (avvisi Dependabot) | REGOLE 8, 9 |
| `go.mod`, `go.sum` (go-ntlmssp) | `github.com/Azure/go-ntlmssp` v0.0.0-20221128193559-754e69321358 -> v0.1.1 come indiretta fissata in `go.mod`; `go-ldap/ldap/v3` resta v3.4.10, perche' la prima che chiede v0.1.1 (v3.4.14) alzerebbe anche `x/crypto` e `asn1-ber`; nessun altro modulo sale | chiude GHSA-pjcq-xvwq-hhpj (avviso Dependabot) | REGOLE 8, 9 |
| `generate_api.go`, `generate_run.go` -> `tools/generate.go` | direttive `go:generate` spostate fuori dal pacchetto radice, con build tag | la radice era un `package main` senza `main`: `go build/vet/test ./...` fallivano | REGOLE 10.2 |
| `lib/lock/local_test.go` | formato costante nei `Fatalf`; niente letture non sincronizzate | i test devono girare in CI con `-race -shuffle=on` | REGOLE 6.1 |
| `.golangci.yml`, `.github/lint-bonificati.txt`, `.github/scripts/riepilogo-lint.py` | golangci-lint v2 in CI: bloccante sui pacchetti bonificati, informativo sul resto | il debito ereditato resta visibile e non cresce | 0012, REGOLE 6.3 |
| `.github/workflows/remotek-ci.yml`, `.gitleaks.toml` | CI del fork: gitleaks, go.sum, build/vet/test, zizmor | controlli a ogni push e PR verso `remotek`, gratis sul repo pubblico | 0012, REGOLE 7 |
| `http/http.go` | `NewEngine()` costruisce il router, `ApiInit()` lo avvia: stesse istruzioni, stesso ordine | i test del contratto usano il router vero senza aprire la porta | 0014, REGOLE 6.1 |
| `test/contratto/` | golden delle risposte di v2.7 alle richieste del client 1.4.9 (52 passi: anonimi, 404 delle cinque richieste che l'API non implementa, utente di collaudo, dispositivo, login errato, login OIDC senza utente e `sysinfo_ver`, ban) e libreria Go di normalizzazione e confronto, stessa semantica del registratore | il contratto col client si verifica, non si ricorda | 0012, 0014, REGOLE 6.1 |
| `cmd/contratto_test.go` | `TestContract`: `InitGlobal()` e router vero su `httptest`, in una cartella temporanea, rieseguono i golden di `test/contratto/` | il contratto si controlla in CI a ogni push e PR verso `remotek`; in `package main` finche' il bootstrap sta in `cmd/` | 0014, REGOLE 6.1 |
| `config/config.go`, `conf/config.yaml`, `http/http.go` (con i test) | default sicuri nel codice e file allineato: web client, `webauth`, autoregistrazione e swagger spenti, captcha dopo 3 login sbagliati, ban dopo 10; `gin.trust-proxy` vuoto vuol dire nessun proxy fidato, non tutti | un'installazione senza configurazione e' sicura, e `X-Forwarded-For` non sceglie l'IP con cui captcha e ban contano un client | 0008 |
| `service/user.go` (con i test) | senza `jwt.key` il token di sessione e' fatto di 16 byte da `crypto/rand` in esadecimale, non piu' md5(nome utente + ora); se `crypto/rand` fallisce non si emette nessun token | il vecchio token si ricostruiva da nome utente e ora del login; la forma (32 caratteri esadecimali) non cambia, il contratto col client nemmeno | 0008 |
| `http/request/admin/user.go` (con i test) | password nuove di 15-32 caratteri in registrazione, password impostata dal pannello (`changePwd`) e cambio della propria (`changeCurPwd`), e in registrazione la conferma uguale alla password; non cambiano il login (client 4-32, pannello senza limiti) ne' la vecchia password nel cambio (4-32) | 15 e' il minimo NIST SP 800-63B-4 per una password che e' l'unico fattore; le password corte gia' impostate funzionano ancora, il contratto col client non cambia | 0008 |
| `cmd/apimain.go` (con i test), `.gitignore`, `docs/init_admin_pwd.png` (tolta) | password iniziale di admin di 20 caratteri in `data/admin-password.txt` (0600), non piu' nel log; riga di Version, gruppi predefiniti e admin in una transazione, e un errore della migrazione ferma il processo; `reset-admin-pwd` e `reset-pwd` accettano solo password di 15-32 caratteri ed escono con 1 se rifiutano o falliscono | la password finiva nel log; un primo avvio interrotto lasciava un database senza admin, che `reset-admin-pwd` non trovava; uno script non vedeva i fallimenti dei comandi | 0008 |
| `resources/i18n/`, `global/i18n.go`, `global/apiValidator.go`, `http/request/`, `config/config.go`, `conf/config.yaml` (con i test) | italiano predefinito (`lang: it`) per messaggi e validatore, col tu e i termini del client RustDesk; una regola sola sceglie la lingua (quella di `Accept-Language` se l'API la ha, altrimenti `lang`, altrimenti l'inglese); un validatore per lingua, coi nomi dei campi tradotti dai file di lingua; i 22 ID che mancavano; un file di lingua che non si carica ferma l'avvio | chi usa Remotek e' italiano e il client non manda `Accept-Language`: i messaggi erano in cinese o in inglese, il validatore rispondeva in inglese a `it-IT` coi nomi dei campi in cinese, dove mancava l'ID l'utente lo leggeva nudo, e un file rotto si saltava in silenzio; i due testi che il client confronta alla lettera non passano da i18n | — |
| `http/response/response.go` (con i test) | `TranslateMsg`, `TranslateTempMsg` e `TranslateParamMsg` passano da un punto solo: un ID che non esiste va nel log a livello warn e al client va `SystemError`; un messaggio che manca in una lingua ripiega sull'inglese | 12 controller passano `err.Error()` a `TranslateMsg` come se fosse un ID: un errore interno, come quello di rete di un provider OIDC, arrivava al client col suo testo | REGOLE 8 |
| `http/response/response.go`, `http/controller/api/`, `http/controller/admin/` (con i test) | `ErrorErr` e `FailErr` rispondono come `Error` e `Fail` col messaggio tradotto del primo ID nella catena dell'errore o, se non ce n'e', di quello del punto, e scrivono l'errore nel log (warn, metodo e rotta); le usano i 36 punti dei controller del client e i 163 del pannello (42 nella sezione dell'utente, 121 nell'amministrazione) che mettevano `err.Error()` nella risposta; un test con go/ast conta le chiamate `X.Error()` rimaste in `http/controller` contro l'elenco dei file da sistemare | un errore interno, come quello di JSON o del database, arrivava al client e al pannello col suo testo, e il lint non lo vede | REGOLE 8 |
| `config/`, `global/`, `http/` (`http.go`, `run.go`, `run_win.go`), `http/controller/web/`, `http/request/admin/`, `http/request/api/`, `http/response/`, `http/response/api/`, `lib/jwt/`, `lib/lock/`, `lib/logger/`, `lib/orm/`, `model/`, `model/custom_types/`, `cmd/apimain.go` (con i test), `.github/lint-bonificati.txt` | bonifica del lint, primo gruppo: 14 pacchetti senza finding di golangci-lint e formattati entrano nel job `lint` bloccante; import ordinati, spazio dopo `//`, doc-comment col nome in testa; tre difetti trovati dal lint corretti: il server che non si apre (porta occupata) ferma il processo con codice 1 e l'errore nel log, il file di log e' 0600 anche se esisteva, gli errori della configurazione nominano il file e avvolgono la causa | il debito ereditato cala e non torna nei pacchetti bonificati; col codice 0 systemd e Docker prendevano un avvio fallito per uno stop normale; il log contiene nomi utente e IP | 0012, REGOLE 6.3 |
| `utils/password.go`, `utils/tools.go`, `service/user.go`, `http/controller/admin/user.go` (con i test) | `VerifyPassword` non accetta piu' l'hash md5(password+"rustdesk-api") delle versioni molto vecchie di rustdesk-api e non lo riscrive in bcrypt: un hash che non e' bcrypt vale come password sbagliata; via `utils.Md5` | un md5 senza sale era ancora una credenziale valida; Remotek non ha mai avuto database cosi', chi ne importa uno usa `reset-pwd` | 0008 |
| `lib/upload/`, `http/controller/admin/file.go`, `config/oss.go` (tolti), `http/router/admin.go`, `global/global.go`, `config/config.go`, `cmd/apimain.go`, `utils/`, `http/controller/admin/login.go`, `.github/lint-bonificati.txt` | bonifica del lint, secondo gruppo: `cmd` e `utils` senza finding e formattati entrano nel job `lint` bloccante; via il caricamento su OSS e in locale, le cui rotte erano commentate gia' in upstream, con `global.Oss` e `config.Oss`; `RequireCaptcha` e `DrawCaptcha` restituiscono l'errore per ultimo | il debito ereditato cala e non torna nei pacchetti bonificati; codice che nessuna richiesta raggiunge non va tenuto in ordine; la documentazione swagger in `docs/admin` cita ancora le rotte di file e non si rigenera qui | 0012, REGOLE 6.3 |
| `lib/cache/`, `config/cache.go`, `config/redis.go` (tolti), `global/global.go`, `config/config.go`, `cmd/apimain.go`, `.gitignore`, `runtime/.gitkeep` | via il client Redis (`global.Redis`) e la cache su file o su Redis (`global.Cache`, `lib/cache`) con `config.Redis` e `config.Cache`; `go-redis/redis/v8` esce da `go.mod`; `runtime/` resta nel repo con un `.gitkeep` suo, non piu' in `runtime/cache/` | `InitGlobal` li costruiva e nessuno li leggeva: codice che nessuna richiesta raggiunge non va bonificato; `runtime/` serve al file di log (`./runtime/log.txt`), che il logger apre senza creare la cartella | REGOLE 6.3 |
| `.golangci.yml`, `cmd/apimain.go`, `http/messaggi_test.go`, `http/middleware/`, `http/router/`, `http/controller/admin/my/` (con i test), `.github/lint-bonificati.txt` | bonifica del lint, quarto gruppo: spento QF1008 di staticcheck, e i due selettori gia' abbreviati tornano espliciti (`AllService.UserService.UpdatePassword`, `AllService.OauthService.SetOauthCache`); `http/middleware`, `http/router` e `http/controller/admin/my` senza finding e formattati entrano nel job `lint` bloccante; `BatchCreateFromPeers` risponde `OperationFailed` alla prima riga che non si salva, invece di successo | i selettori espliciti dicono quale servizio si usa e servono a togliere `AllService` (REGOLE 10.2); il debito ereditato cala e non torna nei pacchetti bonificati; il pannello diceva successo anche con righe perse | 0012, REGOLE 6.3, 10.2 |
| `http/controller/admin/`, `http/router/admin.go`, `http/pannello_test.go`, `.github/lint-bonificati.txt` | bonifica del lint, quinto gruppo: `http/controller/admin` senza finding e formattato entra nel job `lint` bloccante; il logout del pannello (`POST /api/admin/logout`) passa da `BackendUserAuth` e cancella davvero il token, e se non ci riesce risponde `OperationFailed`; `BatchCreate` e `BatchCreateFromPeers` dell'amministrazione rispondono `OperationFailed` alla prima riga che non si salva, invece di successo | la rotta di logout era registrata prima del middleware e lasciava il token valido fino alla scadenza; il pannello diceva successo anche con righe perse; il debito ereditato cala e non torna nei pacchetti bonificati | 0012, REGOLE 6.3 |
| `Dockerfile`, `.dockerignore`; tolti `Dockerfile.dev`, `Dockerfile_full_s6`, `docker-compose-dev.yaml`, `docker-dev.sh` | `Dockerfile` multi-stage dal sorgente: binario Go statico (CGO per sqlite, glibc dell'immagine golang), pannello rustdesk-api-web al commit fissato `3998c2a` con `npm ci` dal registry ufficiale, base Alpine; immagini fissate per digest, utente 10001, `HEALTHCHECK` su `/api/version`, label OCI, `resources/version` da `ARG VERSION`, senza `resources/web`; `.dockerignore` a elenco di ammessi; via le build di upstream (binario compilato fuori, master del pannello con `npm install`, bundle s6) | un'immagine si ricostruisce da commit e digest, senza artefatti ne' branch mobili, e non gira come root | REGOLE 8 |
| `config/brand.go`, `config/config.go`, `conf/config.yaml`, `conf/admin/hello.html`, `http/controller/admin/config.go`, `http/router/`, `resources/templates/`, `resources/brand/` (con i test) | marchio in un posto solo: `brand.name` (`RUSTDESK_API_BRAND_NAME`, default "Remotek") da' il titolo del pannello (`admin.title` vuoto), `{{brand}}` nel benvenuto (in italiano) e il titolo delle pagine OAuth (funzione `brand` dei modelli, registrata nel router); `brand.dir` (default `./resources/brand`) con `logo.svg` e `favicon.svg` provvisori, serviti su `/brand/` senza elenco della cartella; non si rinominano prefisso `RUSTDESK_API_`, sezione `rustdesk:`, rotte `/api/admin/rustdesk/*`, module path | nome e logo si cambiano con una variabile e due file, senza toccare il codice ne' ricostruire | — |
| `Dockerfile` (stadio del pannello) | prima di `npm run build` sei righe del sorgente di rustdesk-api-web al commit fissato si adattano: logo e favicon da `/brand/logo.svg` e `/brand/favicon.svg`, titolo statico da `ARG BRAND_NAME` (default "Remotek"); `logo.png` e `favicon.ico` di upstream tolti; la build si ferma se una riga manca | il pannello mostra il marchio che l'API serve, finche' non c'e' il fork del pannello (A3), dove queste righe si spostano | — |
| `resources/templates/`, `http/middleware/marchio.go`, `http/router/router.go`, `README.md`, `README_EN.md` (con i test) | pagine del login OAuth senza risorse esterne: via Font Awesome da CDN, icone SVG inline; lo script delle traduzioni si crea con `createElement` e l'indirizzo con `encodeURIComponent`, non piu' con `document.writeln`; le risposte di `/brand/*` hanno una `Content-Security-Policy` senza script e in sandbox e `X-Content-Type-Options: nosniff` | il CDN riceveva l'IP di chi apriva la pagina; un messaggio con `"` e `>` diventava HTML nell'origine dell'API, la stessa del pannello; uno SVG del marchio aperto direttamente eseguiva i suoi script nella stessa origine | REGOLE 8 |
| `lib/orm/mysql.go`, `lib/orm/postgresql.go` (tolti), `config/gorm.go`, `config/config.go`, `conf/config.yaml`, `cmd/apimain.go` (con i test), `README.md`, `README_EN.md`, `go.mod`, `go.sum` | l'API usa solo SQLite: via i driver MySQL e PostgreSQL, le loro sezioni della configurazione e le variabili `RUSTDESK_API_MYSQL_*` e `RUSTDESK_API_POSTGRESQL_*`, e la creazione del database MySQL all'avvio; dal binario escono i due driver e i 6 moduli che servivano solo a loro; `gorm.type` che non e' `sqlite` ne' vuoto ferma l'avvio con codice 1 prima di creare `data/rustdeskapi.db` | decisione A3 del titolare: si tengono SQLite, OIDC generico e LDAP, perche' ogni componente tenuto va testato e aggiornato; un'installazione vecchia su MySQL non deve partire in silenzio su un database vuoto | A3 |
| `resources/web/`, `http/controller/api/webClient.go`, `http/response/api/webClient.go`, `http/controller/admin/shareRecord.go`, `http/controller/admin/my/shareRecord.go`, `http/request/admin/shareRecord.go`, `service/shareRecord.go`, `model/shareRecord.go`, immagini del web client in `docs/` (tolti), `http/router/`, `http/controller/web/index.go`, `http/controller/admin/addressBook.go`, `http/controller/admin/config.go`, `http/request/admin/addressBook.go`, `service/addressBook.go`, `service/service.go`, `model/user.go`, `cmd/apimain.go`, `config/`, `conf/config.yaml` (con i test), `.dockerignore`, `README.md`, `README_EN.md` | via il web client: le pagine `/webclient`, `/webclient2`, `/webclient-config/index.js`, le rotte `/api/shared-peer`, `/api/server-config`, `/api/server-config-v2`, la condivisione dal pannello (`shareByWebClient`) e i registri delle condivisioni (`share_record`, tolto da `AutoMigrate`); via `app.web-client`, `rustdesk.webclient-magic-queryonline` e `rustdesk.ws-host`; `/api/admin/config/app` manda `web_client` 0 fisso | decisione A3 del titolare: 15 MB di JavaScript di upstream che non si aggiorna a pezzi e porta la gran parte degli avvisi Dependabot, spento di default e non chiamato dal client nativo 1.4.9 | A3 |
| `resources/i18n/` (tolti `es`, `fr`, `ko`, `ru`, `zh_CN`, `zh_TW`), `global/apiValidator.go`, `global/i18n.go`, `conf/config.yaml` (con i test), `README.md`, `README_EN.md` | l'API parla solo italiano e inglese, nei messaggi e nel validatore; `lang` che non e' `it`, `en` ne' vuoto ferma l'avvio con codice 1 e l'elenco dei valori ammessi | decisione A3 del titolare: ogni lingua tenuta va tradotta a ogni messaggio nuovo; un `zh-CN` di un file vecchio non deve diventare inglese in silenzio | A3 |
| `lib/orm/sqlite.go`, `cmd/apimain.go` (con i test), `go.mod` | `NewSqlite` crea `data/` (0700) se manca e restituisce l'errore invece di stamparlo; un database che non si apre ferma l'avvio con codice 1 e un messaggio col percorso assoluto e cosa controllare; dopo l'apertura una scrittura di prova (una tabella creata e annullata nella stessa transazione) ferma l'avvio con codice 1 se il file o la cartella non si scrivono, col rimedio `chown -R 10001:10001` | prima il processo andava in panic con un errore fuorviante, e `data/` montata non scrivibile da 10001 e' il primo errore probabile di chi installa; SQLite apre in sola lettura senza errore un database che non puo' scrivere, come quello lasciato dall'immagine di upstream che girava come root, e l'API sbagliava alla prima scrittura | - |
| `docker-compose.yaml`, `README.md`, `README_EN.md`, `.gitignore`; tolti `build.sh`, `build.bat`, `debian/`, `systemd/` | via script di build, pacchetto Debian e unita' systemd di upstream; `docker-compose.yaml` costruisce l'immagine dal `Dockerfile` (`build: .`) come `ghcr.io/labinfinitek/remotek-api:dev`, `TZ=Europe/Rome`, segnaposti nelle variabili `RUSTDESK_API_RUSTDESK_*`, dati in `./remotek-data` di 10001:10001, senza `container_name`; i README dicono `go build -o apimain ./cmd` e `docker build` | componenti non provati si tolgono; `build.sh` e `build.bat` riscrivevano l'ambiente Go di chi li lanciava (`GOPROXY` su `goproxy.cn`), il compose usava l'immagine di upstream con fuso e chiave d'esempio | REGOLE 9 |

Contratto verso il client RustDesk: invariato (ogni endpoint chiamato dal
client avra' un test del contratto prima di essere toccato).

Licenza: MIT, vedi `LICENSE`. Segnalazioni di sicurezza: `SECURITY.md`.

---

# What this fork changes (English)
Upstream base: **rustdesk-api v2.7** (unmaintained upstream; this fork is the
project). Changes are listed above with their reason; the client-facing API
contract is unchanged. Licence: MIT, see `LICENSE`; security: `SECURITY.md`.
